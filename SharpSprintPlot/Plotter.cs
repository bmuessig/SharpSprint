using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using SharpSprint.Elements;
using System.Drawing;

namespace SharpSprintPlot
{
    public static class Plotter
    {
        public static Bitmap Draw(Group Group)
        {
            return null;
        }

        public static Bitmap Draw(Element Element)
        {
            if (Element.GetType() == typeof(Circle)) ;
            return null;
        }
    }
}
